language: python

services:
  - mongodb
  - postgresql

sudo: false

matrix:
    fast_finish: true

python:
#  - "3.3"
#  - "3.4"
  - "3.5"

install: "pip install -r requirements.txt"

before_script:
  - createuser -d -l -w
#  - psql -c 'create role ethereumscanner with login createdb;' -U postgres
#  - psql -c 'alter role ethereumscanner with password "qqqwww121";' -U postgres
#  - psql -c 'create database ethereumscanner owner ethereumscanner;' -U postgres

script: ./manage.py test

notifications:
    email: false